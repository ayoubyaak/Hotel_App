framework:
    secret: '%env(APP_SECRET)%'
    session: true

    assets:
        base_path: '/front'

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
